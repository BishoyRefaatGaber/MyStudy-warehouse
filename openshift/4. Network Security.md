## Encrypting Routes
1. Edge
	- TLS termination occurs at the router, before the traffic is routed to the pods.
2. Passthrough
	- encrypted traffic is sent straight to the destination pod without TLS termination from the router.
3. Re-encryption
	- the router terminates TLS with a certificate, and then re-encrypts its connection to the endpoint.
---
## Configure Network Policies



oc annotate service hello service.beta.openshift.io/serving-cert-secret-name=hello-secret
oc annotate configmap ca-bundle service.beta.openshift.io/inject-cabundle=true